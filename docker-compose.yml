services:
  mediview:
    image: node:22-alpine
    working_dir: /app
    ports:
      - "8787:8787"
    volumes:
      - ./:/app
      - mediview_data:/app/data
    command: sh -c "npm install && npm run build && npm run start"
    environment:
      PORT: 8787
volumes:
  mediview_data:
